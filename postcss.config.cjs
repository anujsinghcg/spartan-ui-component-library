/**
 * PostCSS configuration created manually to match `-p` init.
 * This config loads Tailwind CSS and Autoprefixer.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
